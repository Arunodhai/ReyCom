apiVersion: 1

providers:
  - name: ReyCom
    orgId: 1
    folder: ReyCom
    type: file
    disableDeletion: true
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
